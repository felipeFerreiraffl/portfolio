/* ---------- Funções - Menus ---------- */

export const handleOpenMenu = (setter) => {
  setter(true);
};

export const handleCloseMenu = (setter) => {
  setter(false);
};

export function handleMenuGSAP() {}
